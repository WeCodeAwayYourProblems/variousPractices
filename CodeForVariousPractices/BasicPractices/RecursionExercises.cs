using System.Text;

namespace CodeForVariousPracices.BasicPractices;

public class RecursionPractices
{
   // Write a program in C# Sharp to display the individual digits of a given number using recursion.
   public char DisplayDigits(int input)
   {
      // List to be returned
      List<int> returnList = new();

      // Convert the input into a string for manipulation
      string inputString = $"{input}";

      // Determine the total number of digits in the input
      if (inputString.Length == 1)
      { return inputString[0]; }
      inputString[] 1 - inputString[inputString.Length]];
   }
}